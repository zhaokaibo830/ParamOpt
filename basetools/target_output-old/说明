 算法逻辑

任务的特征范围均是[1,50],默认值是0，float型。                                                                                                                                                                                                                                                                                                                                                                                                                                                  

1. 训练每个原任务获取对应的随机森林模型，优化目标是极小值

2. 对每一个原任务训练一个svm分类器

3. 接下来训练目标模型，迭代次数为100次，每次迭代过程如下：

   1. 根据当前目标任务的采样数据和迭代所获取的数据得到当前轮次的随机森林

   2. 获取重要性特征，重要性特征数是8

   3. 计算每个原任务和目标任务之间的相似度

      计算相似度要考虑目标任务当前得所有数据（初始样本+迭代产生的样本）

   4. 根据相似度列表选择5个原任务对应的分类器，其中一个分类器被选中的概率等于其相似度与相似度列表的和之比

   5. 根据目标任务特征的范围和特征重要性进行采样得到采样数据sample_data

      采样和基础调优器一样，重要的参数采用的是覆盖采样，不重要的参数采用的是随机采样，采样数量按照高斯函数衰减。刚开始重要参数采样数量为100，不重要参数采样数量为50。

   6. 用所选中的分类器对所得到的每一个采样数据进行投票，投票结果大于5//2即被选中，最后所有被选中的样本集为condenseed_sample_data

   7. 使用第一步训练好的随机森林模型对condenseed_sample_data样本集进行预测

   8. 找到性能值最优的那一个，接入到实际模型计算一下实际性能值

   9. 合并得到的最优结果到初始数据集